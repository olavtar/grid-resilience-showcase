{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "camera-simulator.serviceAccountName" . }}
  labels:
    {{- include "camera-simulator.labels" . | nindent 4 }}
{{- end }}
