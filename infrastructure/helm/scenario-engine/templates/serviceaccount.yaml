{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "scenario-engine.serviceAccountName" . }}
  labels:
    {{- include "scenario-engine.labels" . | nindent 4 }}
{{- end }}
