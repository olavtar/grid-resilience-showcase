{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "defect-detector.serviceAccountName" . }}
  labels:
    {{- include "defect-detector.labels" . | nindent 4 }}
{{- end }}
