{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "console-frontend.serviceAccountName" . }}
  labels:
    {{- include "console-frontend.labels" . | nindent 4 }}
{{- end }}
